"""Material catalog module namespace; implementation begins at T-07."""

"""Material catalog bounded capability.

The catalog owns typed Material, Material State, and Property Set revisions.  It deliberately
does not expose a generic property key/value model.
"""
