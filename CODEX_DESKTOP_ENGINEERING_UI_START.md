# Codex Desktop Engineering UI Start

`AGENTS.md` is the single execution handoff for a new session. Read its **Current execution handoff**
and `docs/13-delivery/backlog.md`, then take only the next linked issue.

The approved product/visual baseline is `55cfa62` (PR #156). Start every task from the latest `main`
with `git pull --ff-only origin main`. PR #125–#165 are merged scope and must not be reimplemented.
Work proceeds #157 → #158 → #159 → #160 → #161 → #162 with one writer, deterministic gates, a fresh
independent review, and at most one correction/re-review.
Automatic LLM review remains disabled under #119.

For visual work, follow the approved references named in `AGENTS.md`; do not redesign the PR #156
topology. `docs/_incoming/2026-07-24-organic-ux-update/` is temporary reference and must remain until
#162 completes its absorption and zero-inbound audit.
