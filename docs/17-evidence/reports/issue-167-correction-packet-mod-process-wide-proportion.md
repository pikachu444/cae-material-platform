# Issue #167 sole correction packet — MOD-PROCESS wide proportion

Date: 2026-07-31
Writer: configured fresh `correction_terra_high`
Mode: one bounded correction after active-main-agent visual rejection

## Why this correction is authorized

The configured Luna Max writer produced a working responsive SVG implementation and captured a
3840×2160 image before later turns returned the exact capacity error `Selected model is at
capacity. Please try a different model.` The active main agent opened that intermediate original at
3840×2160 and rejected it: stable SVG glyphs and strokes fixed the former distortion, but the plot
still occupied roughly the entire 3,600×1,700 result region and was grossly disproportionate to the
208 px rail and shallow operation ribbon.

This packet is the one configured correction following a material main-agent visual-gate failure.
It is not a model fallback for ACT-RECOVERY, which produced no implementation and remains blocked on
Luna capacity.

## Authority

Read and follow:

- `AGENTS.md`;
- `docs/17-evidence/reports/issue-167-wide-correction-packet-mod-process.md`, including its amended
  `Wide-screen use of space` section;
- the current packet-owned partial implementation and capture/validation changes;
- the approved MOD-DATA graph contract and current MOD-PROCESS state/data contracts named in the
  implementer packet.

## Owned correction scope

You may edit only the current MOD-PROCESS packet-owned static source, capture, validator, staging,
canonical/responsive/state measurements and images. Do not edit the shared manifest, inventory,
product/UI specifications, screenshot archive or freeze report. Do not touch production React/CSS,
other reference families, commits, pushes or GitHub state. Other agents and the user own all
unrelated worktree changes; do not revert them.

## Required result

1. Preserve the completed CSS-pixel SVG renderer:
   finite arrays, data-relative nice bounds, zero anchors, top/right headroom, stable 11/12 px
   typography, non-scaling strokes, complete engineering axis titles, numeric ticks and compact
   collision-free internal legend.
2. Preserve canonical 1366×768, 1440×900 and 1920×1080 plus 2560×1440 as graph-first layouts.
3. At 3840×2160 only, bound the plot to a credible engineering working height and use the recovered
   lower result region for one flat `Processed response` grid.
4. Generate exactly ten grid rows from the same `PROCESS_STRAIN`, three observed-series and
   processed-preview arrays used by the SVG. Columns are `Engineering strain [1]`, the three
   observed stresses and `Processed preview (MPa)`. Do not interpolate, resample, smooth, enrich or
   fabricate.
5. The grid is result evidence, not a third inspector. It contains no prose, badges, internal
   vocabulary or duplicate workflow commands. Use compact 12–13 px table text and ordinary rows.
   All ten rows fit at 3840 without a scrollbar. The grid is absent at the other four viewports.
6. Persist the 3840 topology as an additional lifecycle candidate in the family staging only. The
   active main agent will perform shared inventory/manifest integration after the gates.
7. Preserve the 184/192/208 px rail defaults, shallow ribbon, exact Test Data, independent
   inclusion/visibility, ordered operations, preview/save/invalidation behavior, blocked state,
   genuine long-rail scrolling and all unrelated hashes.

## Active-main-agent full-screen language finding

The first corrected originals pass graph geometry but fail the owner's product-language gate.
Existing normal and blocked copy exposes internal implementation/provenance terms in the primary
workspace: `Mapping Profile`, full `CMP-...` identifiers, `robust_huber`, `immutable output
revision`, `stale pointers`, `Recipe details` and `active job`. This is not acceptable merely
because it predated the wide correction.

Keep exact identities, algorithms, mapping/profile detail, immutable-revision mechanics and
processing recipes reachable in `Advanced / Evidence`; replace the primary workspace wording with
the user's task language:

- selected or saved `Test Data`;
- `calculated method`;
- `processing result`;
- `preview not saved`;
- Fit and Export remain unchanged until the processing result is saved;
- blocked recovery asks the user to return to Data and choose compatible saved Test Data.

Do not mention `Mapping Profile` in the normal or blocked primary workspace. Do not show the full
source ID or algorithm/version. `Revision 1` may remain as a readable exact-revision label.
`Processing details in Evidence` is acceptable as the disclosure link. Status text may use
`No active calculation`, not `No active job`. Preserve the underlying exact source and mapping
contract in data attributes, state evidence and Advanced/Evidence; only the primary wording
changes.

The blocked image must also avoid repeating the same recovery instruction in the source helper,
inline banner and centered recovery panel. Use each layer once: the source field identifies that no
compatible saved Test Data is selected; the inline banner states the blocking requirement; the
centered panel owns the single `Return to Data` instruction and action.

The normal status bar must not repeat `Saved Test Data · Revision 1` in both left slots. The first
slot identifies the active curve set (`DP780 tensile · 293 K`); the second retains the exact
human-readable saved Test Data revision.

## Gates

Call capture and validator `--help` before execution. Then run the full packet capture and validator,
Ruff, Python compilation, Node syntax and `git diff --check`. Validation must prove:

- exact dimensions, paths and SHA-256 values;
- zero console/page/resource errors and zero body/document overflow;
- CSS-pixel SVG geometry, stable typography/strokes and data-derived headroom at all five sizes;
- 3840 plot height is materially bounded relative to the rejected intermediate capture;
- exactly ten numeric grid rows match the renderer arrays;
- the grid is visible only at 3840, has no overflow/scrollbar and does not overlap the legend,
  axes, ribbon, Advanced/Evidence row or status bar;
- canonical and 2560 layouts contain no grid and retain the required graph dominance;
- blocked, loading, error, long rail and keyboard-resize evidence still passes.

Return changed paths, commands/results, candidate/support hashes and any residual concern. Do not
claim visual approval.
