# Issue #167 implementer packet — WAVE-04 / MOD-EXPORT

Date: 2026-07-29
Author: active `/root` Sol XHigh main agent
Writer role: configured `implementer_luna_max`, exactly one writer for this family
Issue: <https://github.com/pikachu444/cae-material-platform/issues/167>

## 1. Bounded outcome

Create the complete six-image `MOD-EXPORT` approval family:

1. `modeling-export-normal-1366x768`
2. `modeling-export-normal-1440x900`
3. `modeling-export-normal-1920x1080`
4. `modeling-export-source-blocked-1440x900`
5. `modeling-export-approximation-blocked-1440x900`
6. `modeling-export-delivered-1440x900`

This is static service-reference work only. Do not change production React/CSS, the common
manifest, inventory, common evidence report, current user-guide screenshots, GitHub or git state.
`MOD-FIT` is the approved dependency. Preserve its shared shell, stage strip, compact navigator and
professional graph grammar. Export changes the primary task from candidate comparison to exact
preflight, native solver-card preview and immutable delivery; it does not redesign the shell.

## 2. Product task and main-agent judgment

An engineer starts from one explicitly selected and saved synthetic material-model result, chooses a
synthetic non-production target/version/unit system, reviews exact/transformed/approximated/
unsupported mappings, previews the native card, acknowledges only applicable approximations and
delivers one immutable solver-card artifact with a receipt.

The graph remains available as exact source context, but the native card and mapping sheet are the
dominant Export result. At 1366×768, do not reproduce the old reference's squeezed graph plus
below-fold delivery sheet. The Export workspace should read as one compact property pane beside one
flexible native-preview region. Preserve the approved Fit graph faithfully in a bounded,
collapsible/resizable source-context area instead of redrawing it with distorted axes, oversized
labels or an external legend tax.

No production solver support, production material family/model/standard, release, approval, review
or Activity projection may be implied. Recommendation, engineer selection, saved Fit result,
delivery and released artifact remain distinct states.

## 3. Authorities inspected by the main agent

- `AGENTS.md`
- `docs/01-product/service-reference-inventory.yaml` (`MOD-EXPORT`)
- `docs/01-product/desktop-engineering-ui-product-spec.md` Modeling Export flow
- `docs/01-product/desktop-engineering-ui-spec.md` E-05–E-08 and Export UI-spec entries
- `docs/01-product/visual-acceptance-matrix.md`, including Q-01–Q-11
- approved MOD-FIT HTML/CSS/JS, images and evidence
- approved MAT-CARD preview/delivery-sheet images and sources
- reference-only prior Export screenshot
  `docs/17-evidence/images/ux-redesign-v2/dui-04-modeling-export-1440x900.png`
- current contracts:
  `apps/web/src/modeling-target-preview.tsx`,
  `apps/web/src/modeling-target-preview-result.tsx`,
  `apps/web/src/modeling-target-delivery.tsx`,
  `apps/web/src/modeling-export-prerequisites.tsx`,
  `apps/web/src/modeling-export-eligibility.ts`,
  `apps/web/src/types.ts`, related API/session code and tests

Approved static sources/images are visual authority. Production sources are read-only contract
evidence. The old Export screenshot is reference only and must not reintroduce its weak vertical
allocation or invented semantics.

## 4. Preserved state and data contracts

| Static region/state | Contract represented |
| --- | --- |
| shared shell/stages | `ModelingStageShell`; Data, Process, Fit, Export only |
| exact source | selected saved Fit result plus exact Processing Output and Neutral revision |
| target | synthetic Abaqus/OpenRadioss 2025 choice and kg-m-s unit system |
| source-key invalidation | changing Processing Output or Neutral revision invalidates preview and delivery |
| preflight | exact/transformed/approximated/unsupported mapping categories with actionable blockers |
| preview | ephemeral native-card preview; not saved, reviewed, approved, released or delivered |
| acknowledgement | required only for the exact approximation identity shown by preflight |
| delivery | immutable Solver Card plus delivery receipt; no mutation of the source revision |
| delivered navigation | real preview/card link and receipt link only; no fabricated Activity entry |

Preserve the current truth that Download belongs to the resulting Materials solver-card surface
unless a current contract exposes a direct download. Do not invent a direct download API. UUIDs,
hashes, mapping-profile JSON, checksums and full evidence remain in Advanced/Evidence.

## 5. Family visual contract

### Shared shell and source continuity

- Keep the approved dark 46 px shell, compact title/context row and
  `Data | Process | Fit | Export` stage strip with Export active.
- Retain the approved responsive Modeling navigator widths and calmer Materials-like hierarchy
  grammar: sentence-case metadata, 12–13 px regular/medium identities, stable indentation, restrained
  selection and local scrolling.
- Show the exact selected saved model and source revisions in user language. Do not expose raw IDs
  in the normal surface.
- Keep graph/source context mounted in a bounded collapsible or resizable region. Its axes use real
  responsive geometry, proportional data headroom, compact professional typography, canonical
  quantity/unit labels and an in-plot legend that does not cover curves.

### Export dock

- Use one flat split workspace, approximately 300–360 px properties/preflight on the left and a
  flexible dominant native-card preview on the right. Alignment and dividers precede cards.
- The left pane groups source, target/version/unit, preflight counts and the current safe action.
  It scrolls locally when required without hiding the action or changing preview width.
- The right pane shows the approved MAT-CARD-like native fixed-width text preview in a monospace
  viewport with independent horizontal/vertical scrolling, plus an adjacent compact mapping sheet.
- Avoid nested cards, decorative gradients, repeated eyebrow labels, KPI tiles, non-status badges
  and a permanent third inspector.
- One filled primary action at most. Disabled actions always have an adjacent reason.

### Normal preview-ready at three viewports

- Exact selected saved model, target and current preflight are present.
- Preflight includes internally consistent exact/transformed/approximated/unsupported counts and
  enough mapping rows to explain them. Unsupported blockers are absent.
- Native preview is already current and explicitly ephemeral/not delivered.
- `Deliver native card` is the sole filled commit action. Secondary actions may regenerate preview
  only when the current contract warrants it.
- Approximation acknowledgement is either already satisfied for the exact current identity or no
  approximation requires acknowledgement. Do not make the normal state silently blocked.
- The native preview and mapping sheet dominate; graph source context remains useful but secondary.

### Source-blocked at 1440×900

- Preserve the complete shell and Export workspace topology.
- Name the exact missing prerequisite: no selected/saved compatible Fit result or its exact source
  revisions. Never fall back to first/latest.
- Do not show a believable native preview, preflight success or delivered artifact.
- Preview/delivery actions are disabled with an adjacent reason.
- Offer one safe recovery command, such as `Back to Fit`, while preserving the Modeling session.

### Approximation-blocked at 1440×900

- Preserve the exact selected model, target, preflight, native preview and mapping context.
- Show the specific current approximation identity and consequence in plain engineering language.
- The acknowledgement is visibly unchecked; delivery is disabled with the unmet requirement next
  to it. Do not substitute a generic warning banner.
- Changing target/source invalidates the acknowledgement; this must be deterministic evidence.

### Delivered at 1440×900

- Preserve source, target, native card and mapping context.
- State that one immutable solver card was delivered, with a compact receipt summary.
- The one primary command opens the real delivered card/preview route; receipt is secondary.
- Do not imply review, approval, release, production readiness or automatic Activity creation.

## 6. Evidence-only states and interactions

Persist independently named PNGs and measurement/state JSON at 1366×768, 1440×900 and 1920×1080
for:

- no-target empty: exact source remains, no preview/delivery, one target-selection next command;
- preflight or delivery loading: prior source, settings and current preview remain, duplicate commit
  is blocked and progress is locally announced;
- delivery error with preflight preserved: exact source/target/preflight/preview remain; retry is
  available and the UI does not call the artifact delivered;
- long mapping disclosure: local pane/text-preview scrolling, full identities accessible, graph and
  preview topology unchanged.

Record deterministic interactions:

- target/source change invalidates preview, acknowledgement and delivery pointer;
- preview completion does not deliver;
- acknowledgement applies only to its exact approximation identity;
- delivery cannot duplicate-submit and success creates a distinct immutable receipt;
- source-context collapse/resize, native-preview scrolling and mapping scrolling have real pointer,
  keyboard and ARIA consequences;
- graph remains responsive without SVG stretch or quantity-label distortion.

## 7. Source ownership

The writer may create/edit only MOD-EXPORT WAVE-04 paths:

- `docs/00-research/ux-service-reference/modeling-export-normal.html`
- `docs/00-research/ux-service-reference/modeling-export.css`
- `docs/00-research/ux-service-reference/modeling-export.js`
- `docs/00-research/ux-service-reference/capture_modeling_export_wave04.py`
- `docs/00-research/ux-service-reference/validate_modeling_export_wave04.py`
- `docs/00-research/ux-service-reference/modeling-export-wave04.staging.json`
- MOD-EXPORT-only PNG/measurement/state evidence under
  `docs/17-evidence/images/issue-167-service-reference/`

Reference shared sources read-only. Do not edit approved images/sources, shared CSS/JavaScript,
manifest, inventory, common evidence, production files or ACT-QUEUE files. Another writer is active;
preserve all unrelated edits and never reset, clean, stash, discard or overwrite them.

## 8. Deterministic and qualitative gates

The family validator must fail unless:

- all six approval images have exact viewport/dimensions/device scale, hashes and pending lifecycle;
- MOD-FIT and MAT-CARD approved prerequisite hashes remain unchanged;
- one compact navigator, one bounded source context and one dominant Export preview exist, with no
  third inspector or nested-card hard-gate failure;
- properties pane is 300–360 px at 1440, preview remains usable at 1366 and expands at 1920;
- fixed-width card preview and mapping sheet scroll locally with no clipping/body overflow;
- source, target, preflight counts/rows, acknowledgement identity, actions and state labels are
  internally consistent;
- source-blocked, approximation-blocked and delivered semantics match the contracts above;
- graph geometry is responsive, axis ranges are data-relative and label/legend/curve containment
  passes;
- semantic controls, focus-visible, names, busy/error announcements and keyboard behavior pass;
- zero console/page/resource errors and zero document/body overflow;
- the legacy-selector report is zero for `page-stack`, `page-heading`, `content-card`,
  `module-material-card`, `hero-actions`, `eyebrow`, `status-badge`, `count-chip`.

Before returning, apply Q-01–Q-11 from the visual acceptance matrix to every original-resolution
approval image and applicable evidence state. Explicitly record pass/fail/not-applicable with
evidence; numeric scores or automation do not replace qualitative inspection.

Run both helpers with `--help` before source, then at minimum:

```text
uv run --with playwright python docs/00-research/ux-service-reference/capture_modeling_export_wave04.py --all-packet-targets
uv run --with playwright python docs/00-research/ux-service-reference/validate_modeling_export_wave04.py --all-packet-targets --expect-main-agent-status pending
uv run python docs/00-research/ux-service-reference/validate_modeling_fit_wave03.py --all-packet-targets --expect-main-agent-status accepted
uv run python docs/00-research/ux-service-reference/validate_service_reference_inventory.py
uv run ruff check docs/00-research/ux-service-reference/capture_modeling_export_wave04.py docs/00-research/ux-service-reference/validate_modeling_export_wave04.py
node --check docs/00-research/ux-service-reference/modeling-export.js
git diff --check
```

If an approved dependency validator uses product-owner terminology rather than `accepted`, use its
documented approved expectation; never edit a dependency to force a pass.

## 9. Handoff

Return exact changed files, commands/results, six image paths/viewports/SHA-256 values, evidence
paths, the Q-01–Q-11 self-review and residual risks. Open all six targets plus representative
evidence images at original resolution. Do not edit integration files, request product-owner
approval, commit or start another family.
