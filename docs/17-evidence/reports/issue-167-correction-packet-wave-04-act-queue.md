# Issue #167 sole correction packet — WAVE-04 / ACT-QUEUE

Date: 2026-07-29
Author: active `/root` Sol XHigh main agent
Correction role: fresh configured Terra High, sole correction for this family
Issue: <https://github.com/pikachu444/cae-material-platform/issues/167>

## Boundary

Correct the Luna Max ACT-QUEUE output in place. Read the original implementer packet and this packet
in full. Retain the flat dense queue topology, role separation, truthful current data boundary,
visible local scroll rail and all passing responsive/state evidence. Do not redesign the family or
edit MOD-EXPORT, production, shared manifest/inventory/common evidence, GitHub or git state.

Owned paths are exactly the ACT-QUEUE paths in section 10 of
`issue-167-implementer-packet-act-queue-wave-04.md`.

## Main-agent original-image rejection

The main agent opened all seven approval images at original resolution. The six normal User/Reviewer
images are visually accepted for correction scope: they are flat, readable, role-truthful and
queue-dominant. The canonical Reviewer long-decision-error image is rejected for contradictory
decision recovery:

1. A non-empty preserved reason is visible, but the footer still says
   `Reason is required before submission.` Replace this with state-aware copy such as
   `Reason retained; retry will resubmit this decision.` The normal empty-reason form must retain the
   required-reason instruction and validation.
2. The long error image shows both filled `Record decision` and secondary `Retry`, creating two
   competing recovery commands. For service failure, retain the selected decision and reason, expose
   one filled `Retry decision` command and remove/hide the duplicate Retry. Its pointer/keyboard
   consequence must actually attempt the same decision and preserve the form on another failure.
3. The page-level status remains `Ready` while the inline review service is unavailable. Set the
   queue status to `Decision not recorded` (or equally precise error state) on initial
   decision-error/long-decision-error evidence and after the failed retry.
4. Make the selected Approve/Request changes choice perceptually clear as well as `aria-pressed`;
   keep it restrained and do not introduce a badge/card treatment.
5. Strengthen the validator to assert these exact state differences and to fail if non-empty reason
   coexists with required-reason help, if two retry/submit commands compete, or if the top status
   says Ready in an error state.

For stale/unauthorized evidence, preserve the selected request, decision and reason but do not imply
that an unauthorized retry can succeed. Use a safe Refresh/access-recovery treatment distinct from
the transient service-error retry and validate it.

## Preserve

- all six normal approval images' topology, density and role semantics unless a small shared-source
  change is needed for the correction;
- no decision controls for User;
- Reviewer row-specific action and non-empty reason requirement;
- no fabricated person/item names, owner, publish, release or dashboard/KPI content;
- visible reserved queue scrollbar in real overflow states, no fake rail otherwise;
- selected request/reason/current rows across error;
- one filled primary action at most, zero page overflow and accessible pointer/keyboard behavior;
- all seven state bundles at 1366×768, 1440×900 and 1920×1080.

## Required commands and handoff

Run both helpers with `--help`, recapture all seven targets and every state bundle, then rerun the
family validator, inventory, Ruff, Node syntax and scoped diff checks. Open all seven corrected
approval images and representative state evidence at original resolution. Recompute Q-01–Q-11
without overclaiming graph/Fit-only items.

Return changed paths, commands/results, seven SHA-256 values, state-evidence paths, Q results and
residual concerns. Do not edit shared integration files, request approval, commit or start another
family.
