# Issue #167 ADM-SCHEMA-CORE governed-identity correction packet

Date: 2026-07-31

## Authorization and boundary

The product owner authorized continued work on dependency-independent #167 reference families while
away. The configured ADM-SCHEMA-CORE implementer/correction cycle was already exhausted, and the
active main agent confirmed the later fresh-review failure recorded in freeze-report section 61.
The active main Sol therefore owns this one-line product correction and its necessary deterministic
evidence updates directly. No writing subagent or model substitution is authorized.

Owned paths:

- `docs/00-research/ux-service-reference/administration-schema-core.js`
- `docs/00-research/ux-service-reference/capture_administration_schema_core_wave05.py`
- `docs/00-research/ux-service-reference/validate_administration_schema_core_wave05.py`
- `docs/00-research/ux-service-reference/administration-schema-core-wave05.staging.json`
- the three `administration-attribute-long-invalid-{1366x768,1440x900,1920x1080}` image and
  measurement pairs;
- the three `administration-long-scroll-{1366x768,1440x900,1920x1080}` evidence images;
- the one long-invalid manifest lifecycle and common freeze report, integrated serially by main.

All other Administration, Materials, Modeling, Activity, production React/CSS and approved image
paths are frozen.

## Required correction

In the `attribute-long-invalid` state, keep the object-list Name cell and accessible row name equal
to the governed stored identity `Material condition`. The deliberately long invalid draft value
belongs only in the editable `Attribute name` field. Never substitute it into the adjacent object
list merely to demonstrate ellipsis.

Preserve:

- the selected `material-condition` row, `Discrete choice`, revision `3`, and editor draft;
- the long draft, validation errors, disabled Save and complete local-scroll behavior;
- `Name | Value type | Rev`, full-width row alignment, and all responsive topology;
- existing wide Layout/Record projection, link/revision truth and all unrelated hashes.

## Deterministic gate

The validator must explicitly prove, at all three long-invalid viewports, that:

- selected row identity is exactly `Material condition`;
- the full governed identity fits and is not ellipsized;
- the editor still contains the distinct long invalid draft;
- Name, Value type and Rev cells do not overlap;
- genuine editor overflow retains the visible reserved, proportional, operable rail.

Call capture/validator `--help` before execution, recapture the bounded long-invalid and long-scroll
evidence, update only the affected manifest hash/lifecycle, then run the complete Administration
validator, inventory validator, Ruff, Python compilation, JavaScript syntax and `git diff --check`.
The active main agent opens every changed image at original resolution, followed by one fresh
read-only Terra High re-review and a repeated final main-agent judgment.
