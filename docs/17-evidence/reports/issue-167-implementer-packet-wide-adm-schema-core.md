# Issue #167 wide-screen correction packet — ADM-SCHEMA-CORE

Date: 2026-07-30
Writer: one configured `implementer_luna_max`
Authority: GitHub #167, `AGENTS.md`, the current pending ADM-SCHEMA-CORE bundle, and the cumulative
checklist in `docs/01-product/visual-acceptance-matrix.md`.

## Bounded objective

Build on, and do not discard, the current dirty ADM-SCHEMA-CORE handoff. At 1920×1080 the navigator
and Object list are appropriately bounded, but the right pane shows an 820 px property sheet inside a
1,256 px region and leaves the rest blank. Use that wide region for the real user consequence of
schema work: a Layout-driven Record datasheet preview synchronized with the current Table/Attribute
selection and local draft.

Preserve the 1366×768 and 1440×900 pixels. Correct the three pending 1920 normal/Table-draft/
Attribute-draft approval targets and all 1920 responsive state evidence. Add 2560×1440 and 3840×2160
normal-state evidence without increasing the 72-image inventory.

## Exact owned paths

The writer may change only:

- `docs/00-research/ux-service-reference/administration-schema-core.html`
- `docs/00-research/ux-service-reference/administration-schema-core.css`
- `docs/00-research/ux-service-reference/administration-schema-core.js`
- `docs/00-research/ux-service-reference/capture_administration_schema_core_wave05.py`
- `docs/00-research/ux-service-reference/validate_administration_schema_core_wave05.py`
- `docs/00-research/ux-service-reference/administration-schema-core-wave05.staging.json`
- ADM-SCHEMA-CORE PNG/measurement paths already generated by that capture script
- new, clearly named 2560×1440 and 3840×2160 normal wide-evidence PNG/measurement siblings.

Do not edit shared product policy, the common manifest, the common issue report, production React/CSS,
Materials paths or any later Administration family. Preserve every unrelated existing modification;
do not reset, clean, stash, overwrite wholesale or revert another writer's work.

## Preserved authority and contracts

- Keep `Schema objects ⇆ Object list ⇆ Property editor / preview`; never add a fourth persistent
  inspector.
- Keep the flat pane/divider grammar, compact 12–13 px rows, identity-only Table Name cells and
  `Name | Value type | Rev` Attribute rows.
- Keep current Add Table/Add Attribute drafts, type-conditional fields, immutable-revision wording,
  local dirty state, save/error/conflict recovery and visible scroll-rail behavior.
- Keep Table scope and list selection while previewing.
- The preview is driven by an explicit Layout field order and a saved synthetic Record; it is not a
  default hard-coded “basic attributes” card. A selected Attribute is highlighted in the preview
  only when it belongs to the current Layout.
- Exact record/revision links remain immutable; hashes, ETags and internal identifiers stay out of
  the normal surface.
- Preserve the current lower-viewport candidate bytes exactly, including:
  - normal 1366 `9995b53dae3a9907fe95f33ad9eed0b4a96a19fe1d7e7d19f61f89249f313724`
  - normal 1440 `1b2491632ca17a96bbcd32efeac6d8d4cc5555b5ee43eaaa016085538828a2bf`
  - Table draft 1366 `9de662dd7dfa2453a66c0b0da830193b4061c25796406b3d88803f8ec5fc8c69`
  - Table draft 1440 `2390d47c2b9828f9aa4ae2a0d47d1829b2b4567c2584f13aac5863d0561cb284`
  - Attribute draft 1366 `e6682346823355eb99da5eb72eb5c795a31b4847a025d5f554a572e607d7dfd0`
  - Attribute draft 1440 `3db6cd5a26221bf62d13bcedd07c7d3a309df3984ef81914a5828da47f9a1a62`
  - stale conflict 1440 `e64c034fb1ad3fd6428ca319d91bde6ec7c675b95b5332d7cb2db49a9552cd21`
  - long invalid 1440 `51157e7802a56e093d228a74770cd43b6ad85bc7cb4be2161eca1859087f3994`

## Required correction

1. At `min-width: 1700px`, keep the property form/read-only definition in a bounded left portion of
   the right pane and place a flat, adjacent `Record preview` region in the remaining width.
2. The preview must name the current Layout and representative saved Record and render a compact
   field/value table from an explicit ordered Layout projection. Use the existing synthetic Materials
   context; do not invent UUIDs, scores, checksums or unrelated fields.
3. The preview must visibly demonstrate user-selected Attributes, for example the ordered current
   Layout fields and their value types/values. Attribute selection/draft updates the corresponding
   preview row or label without changing the saved Record. A new Table truthfully has no Record
   preview until saved.
4. Keep property editing and the preview in one right-pane topology. The preview is not a nested card,
   modal, dashboard tile or permanent fourth inspector. Use divider/alignment first.
5. At 1920, stack preview subsections if needed to retain readable values. At 2560 and 3840, use the
   additional width for side-by-side `Record values` and `Layout fields` projections rather than
   stretching one sentence or form row across the display.
6. Keep the command `Preview datasheet` meaningful: on wide screens it focuses/toggles the adjacent
   preview with an accessible state; smaller approved captures stay unchanged.
7. Preserve independent list/editor scrolling, keyboard consequences, focus visibility, no document
   overflow and no scrollbar/text collision.

## Static-to-production mapping

- Layout name/order/type projection → `ConfigurableCatalogRecords` layouts and ordered Attribute
  definitions in `apps/web/src/configurable-catalog-records.tsx`.
- Saved Record values → existing configurable Record response and exact Attribute Definition
  revision pins.
- Datasheet preview → `MaterialDatasheetProjection` / real Layout-driven Record projection in
  `apps/web/src/material-datasheet-projection.tsx`.
- Table/Attribute drafts → existing create/revise service contracts; a local draft affects preview
  only and never mutates the saved Record.

This packet changes static reference authority only. Do not edit production code.

## Deterministic evidence and gates

- Recapture the complete existing family/state matrix.
- Capture 2560×1440 and 3840×2160 normal-state wide evidence.
- Measure navigator/list/property/preview widths, preview visibility, ordered fields, selected-row
  synchronization, internal subsection topology, all local scroll regions and page overflow.
- Assert the preview is absent from lower canonical pixels and all listed lower hashes are unchanged.
- Assert normal/Table-draft/Attribute-draft 1920 targets retain one three-pane workspace with one
  right-pane editor/preview split, not four persistent workspace columns.
- Exercise Preview toggle/focus, Table/Attribute selection, conditional draft fields, splitter,
  scroll rail and current save/recovery interactions.
- Run the existing validator, Ruff, JavaScript syntax validation and `git diff --check`.
- Return changed paths, exact commands/results, all changed approval image paths/hashes, both wide
  evidence paths/hashes and residual risks.

Do not commit, push, open a PR, edit GitHub or request product-owner approval.
