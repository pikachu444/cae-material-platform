# Issue #261 M1E4 Modeling common core/stage CSS ownership

## Status

Main acceptance, requirements audit, and final independent review are complete for the bounded M1E4
ownership move. The task owner pre-authorized the publication lifecycle; no physical-4K approval is
claimed.
The structured evidence and complete image index are in
[`images/issue-261-m1e4-modeling-core-stage-ownership/manifest.json`](images/issue-261-m1e4-modeling-core-stage-ownership/manifest.json).

The accepted audit contains 172 selector rows in 137 rule groups: 168 common-core/workbench/stage
rows and four engineering-curve plot rows. It records 112 complete groups, 25 mixed groups, 44
preserved non-target peers, and 60 deferred rows in 51 groups. The ordered extended-tuple digest is
`f2b46c9b70df293976ca55f182cc8ffc0715d8d6c5f535b5232f779c79f78aca`.

The independently audited first candidate contained 174 rows in 139 groups. Live CSSOM and pixel
evidence found that moving CSS-1618 and CSS-1619 changed the elastomer chart tick cascade from 12px
to 9px at all five viewports. That attempt is retained only under `live/correction-attempt/`. The
same writer restored the two rules byte-exact at their original `styles.css` position, and the final
candidate narrowed by two rows/two groups. This bounded correction did not widen the ownership model.

The final legacy complement is 1,449 selector rows in 1,231 rule groups, with the 60-row/51-group
M1E residual retained in its real Materials, import, export, test-JSON, cross-route, or other producer.

## Accepted user journey and preservation

The isolated synthetic DP780 journey opened the exact selected Test Data in Data, continued through
Process, compared and explicitly retained `swift + voce 50/50` in Fit, and reached the existing
Export target/native-preview surfaces. The canonical Modeling alias, reload state, and Materials
lineage read-back were also exercised. Dynamic IDs, revisions, resource URLs, and hashes were read
within the disposable run and were deliberately not frozen as reusable constants.

The visible result remains the existing flat Modeling shell: compact explorer, shallow settings
ribbon, Data → Process → Fit → Export navigator, and dominant persistent engineering graph. DOM,
copy, URLs, API contracts, state, persistence, exact-revision behavior, recovery behavior, and the
four-stage order are unchanged. Blocked and negative states remain fail-closed; no latest, first-item,
global-output, cross-route, or other-session fallback was added.

Fourteen live states have accepted browser evidence: the four normal stages, Data empty and invalid
mapping, Process prerequisite blocked, Fit stale/no-selection blocked, Export source blocked,
approximation blocked and delivered, the compatibility alias, reload read-back, and Materials browse
lineage read-back. The remaining registered loading/error edge states are bounded by the durable
source and Storybook oracles and are not misrepresented as live captures.

## Visual and runtime evidence

The evidence index registers 924 PNGs: 702 accepted Main evidence images and 222 non-acceptance
diagnostics. The canonical frozen-base/corrected comparison contains 48 Product/Storybook records and
220 before/after image pairs. It reports zero geometry/text failures, 34 pixel-exact records, 192
pixel-exact images, 28 generated-identity-only images, 220 behavior-preserving images, and zero pixel
failures. The generated-identity deltas preserve body text, geometry, computed style, and behavior.

Main opened the canonical 220 frozen-base images and 220 corrected-candidate images at original
resolution, plus all 40 current-product views and the canonical Storybook pair. Required header,
navigator, table/form, stage-control, graph, and native-preview crops were inspected at direct
100%-pixel resolution. Byte-identical duplicate lifecycle paths are declared as exact complete hash
groups in the manifest.

The canonical Storybook `EngineeringCurvePlot/EmptyCompatibleSeries` pair is exactly 1440×900 at
browser zoom 100% and DPR 1. Both images have SHA-256
`958101d900c3a6e9dc7c64bd86eaea39e3f0a9ba1fca4b224e859f38fb89ef69`.

All five CSS viewports—1366×768, 1440×900, 1920×1080, 2560×1440, and 3840×2160—preserve visibility,
clipping, wrapping, exact identity/revision text, interaction reachability, and layout bounds. The
mandatory #249 synthesis passes all three axes: information hierarchy, engineering task flow, and
responsive/wide-screen composition. Graphs and previews use the available width; navigation, forms,
and prose keep readable bounds. No one-sided work island, unrelated internal void, clipping, or
route-specific high-DPI workaround was introduced.

The disposable Compose project was `cmp-demo-test-m1e4evi`. Seeding was stable on both passes across
304 tables. The pre-existing `cmp-local-demo` volumes and stopped-state record were not changed.
Automated 3840×2160 evidence proves CSS geometry only; physical Windows 4K readability remains
`DEFERRED_TO_223`.

## Durable gates

The accepted state passes the M1E4 source and bundle ownership oracles, CSS inventory and historical
M1E integration regressions, production and Storybook builds, frontend guard, full web tests,
user-guide contract, docs-impact, and diff hygiene. The bundle oracle proves all 172 target identities
are partitioned into the existing core/plot owners and all 60 deferred identities remain exactly once.
The requirements auditor approved with no omissions. The independent reviewer requested two bounded
evidence-contract corrections—the full guide-test provenance assertions and a truncated Storybook
digest—and approved after the 47/47 guide suite and strengthened file-hash validation passed.

The deterministic transform and source tests preserve declarations, `!important`, at-context,
specificity, source order, complete/mixed boundaries, retained peers, complements, import/bundle
consumers, and duplicate effects. The unit adds no class rename campaign, route workaround, styling
framework, CSS zoom/transform, golden refresh, or unrelated M1A/M1B/M4/HOLD/M6 work.
