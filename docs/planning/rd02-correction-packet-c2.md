# RD-02 correction packet C2 — complete the observed workspace and neutral path

Continue the same correction role and the approved v4/C1 scope. No new product scope, engineering
calculation, publication or backend migration is authorized. Preserve the passing exact primary path.
Main now releases the runtime freeze for this complete packet, not only the three abbreviated
findings in the progress commentary. Reopen the accepted material-workspace HTML/CSS and these actual
originals before editing: `.artifacts/rd02/c1-checkpoint/` Materials list, Test Data preview/expanded,
Solver Card detail, Processing primary, Model primary and neutral-card1920 images.

## Accepted evidence to preserve

`c1-checkpoint/primary-chain.json` passes the real UI source→two exact outputs→primary exact model→
exact13 cards→native bytes journey, with zero page errors and the two unrelated same-material cards
excluded. Main's 52 backend tests, read contract lint/compat, actual RLS, M1 nonuniform curve,
385px scroll/focus/URL return, actual missing404, late response isolation and three download recovery
checks remain valid for their recorded sources. Implementation reported11 files/36 frontend tests,
typecheck, live/prototype builds and diff check passed. These do not accept the remaining visual gaps.

## Four concrete corrections

1. **Use the complete workspace area and real hierarchy.** Materials without a selected preview still
   reserves420px at1920 (`materials-list-1920x1080.png`); the same unconditional `resultsWithPreview`
   wrapper exists in Processing, Models and Cards. Apply the optional-preview contract to all readers.
   Native preview currently stops at250px while the expanded pane leaves a large blank lower region;
   let its normal preview consume useful available height through shared layout/preview tokens.
   Keep bounded prose/forms and actual curve geometry, with no route-specific4K rules or scaling.

   The explorer is still a flat material-button list followed by separate state links, or a combined
   material/method-button list. It is not the accepted expandable material→state→specimen/Test Data
   tree with separate filters. Implement that stored hierarchy and useful collapse/selection behavior.
   Exact Test Data read/list `governed_source.material` and `governed_source.material_state` provide
   aggregate/revision pins; `specimen_id` supplies the stored specimen label. See
   `live/exact-test-data-detail.json`: primary material ea039cf1…, state821a7b8f…/3c2836dd…,
   specimen01. Keep unlinked data explicitly unlinked; never infer a state by matching a name.
   A paginated Materials tree may expose its server page scope but must not claim a complete global
   tree. Direct Test Data/Card readers stay usable without Materials/Catalog permission. Separate
   name/code typography (the current tree concatenates them) and remove filler revision/Current strips.

2. **Actually move compatibility information out of normal surfaces.** The originals still show
   EXPANDED EXACT DETAIL, full pinned UUIDs, identity-policy prose, URN model headings, schema/state
   UUID fields and Mapping Profile on normal surfaces. Processing stage diagnostics occupy a very
   long normal table and bury the useful relation links. This is an uncompleted C1 item, not a new
   copy request. Normal surfaces should show human-readable model/method names, stored quantities and
   units, concise conditions/warnings, source/result distinction and clear actions. Put full schema,
   pins, technical channel/method keys, mapping/provenance and raw diagnostics in Evidence/Advanced.
   Preserve exact immutable native text, including its metadata comments: native artifact bytes are
   not UI copy. Replace Model IR/“33 typed fields returned” filler with a bounded useful summary of
   available stored model parameters; do not invent missing parameters or engineering defaults.
   Clearly label the input quantity separately from the final plotted quantity: current Processing
   says independent `strain.engineering` while its final x-axis correctly shows true plastic strain.

3. **Complete the actual neutral generation path.** The actual neutral-card detail has no relation
   anchors at all beyond scope tabs (`primary-chain.json.neutral.links`). An identifier in Evidence
   does not satisfy a usable reverse relation. Provide Card→exact neutral model→its exact Processing
   Output/Test Data and neutral model→its exact two cards. Also expose the seed output→neutral model
   edge, separately from the seed tabulated-model membership. The exact baseline is
   `live/expected-primary-relations.json`; do not invent a tabulated→neutral edge.

   Existing read contracts are sufficient: `/bulk-export-candidates?material_id=...` has a
   `neutral_material_json` source with both neutral ID and revision (`live/metal-export-candidates.json`).
   Read the exact neutral JSON using
   `/neutral-materials/{id}/revisions/{revision_id}/download` and verify the returned neutral identity/
   revision headers and document identity through a typed model adapter. This endpoint's declared
   X-Content-SHA256 is the Neutral document semantic digest, not the complete response-byte digest;
   Main observed200 and matching neutral ID/pin but a different raw-body SHA. Honor its existing
   document hash contract; do not reuse the generic artifact-byte validator incorrectly or change the
   backend hash semantics. The current direct card payload already records neutral ID+revision.
   Candidate/model reads are optional permission boundaries and must fail locally without blocking
   valid source or native-card reads. No raw API link requiring a lost bearer session is a UI relation.

4. **Complete the already-frozen filters and prove the changed outcomes.** Test Data now has sort/type;
   Processing still only has search and stage shortcuts, Cards search/family only. Implement their
   v4 local post-filter scope, sort and12-row paging with accurate counts and off-page close. Keep
   Materials row/count/facet server scope. Add focused regressions for the missing tree selection,
   no-preview width across shared readers, normal-copy disclosures, exact neutral relations and
   affected filters; retain the existing36 tests and Main live evidence. Run typecheck/live build and
   affected tests, then send Main one complete C2 checkpoint with any remaining failures.

Main owns the actual chain/browser/original review, source-head advance, final20 originals plus
minimum/maximum negative states and crops, current guide/manifest and canonical independent review.
The final five-viewport set remains deferred while these missing states/composition make it invalid.
This is the second consolidated correction packet; do not narrow it to a convenient subset or reopen
passing backend calculations. No additional permission is needed for these already-authorized fixes.
